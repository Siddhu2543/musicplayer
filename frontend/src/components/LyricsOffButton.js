import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faClosedCaptioning } from "@fortawesome/free-solid-svg-icons";

const LyricsOffButton = () => {
  return (
    <FontAwesomeIcon icon={faClosedCaptioning} size="sm" title="Lyrics Off" />
  );
};

export default LyricsOffButton;
