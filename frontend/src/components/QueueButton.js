import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faIndent } from "@fortawesome/free-solid-svg-icons";

const QueueButton = () => {
  return <FontAwesomeIcon icon={faIndent} size="sm" title="Queue" />;
};

export default QueueButton;
