import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faCirclePlay } from "@fortawesome/free-solid-svg-icons";

const PlayButton = () => {
  return <FontAwesomeIcon icon={faCirclePlay} size="2xl" title="Play" />;
};

export default PlayButton;
